package ${Contest.PackageName};

import java.io.*;
import java.util.*;

public class ${ClassName} {
    public ${Method.ReturnType} ${Method.Name}(${Method.Params}) {
        return ${Method.ReturnType;zeroval};
    }

    static void debug(Object... o) {
        System.err.println(Arrays.deepToString(o));
    }

    ${CutBegin}
    ${<TestCode}
    ${CutEnd}
}